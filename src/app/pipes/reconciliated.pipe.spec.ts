import { ReconciliatedPipe } from './reconciliated.pipe';

describe('ReconciliatedPipe', () => {
  it('create an instance', () => {
    const pipe = new ReconciliatedPipe();
    expect(pipe).toBeTruthy();
  });
});
